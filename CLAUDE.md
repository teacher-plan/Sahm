# Workflow

For every code edit in this repo: commit and push automatically to the
`claude/project-review-p1szev` branch without asking first.

Opening a PR into `main` and merging it is a separate, explicit step —
ask for confirmation before merging each time, even though pushes happen
automatically.
